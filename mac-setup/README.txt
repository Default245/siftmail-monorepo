# Sift Mac Setup (One‑click)
Double‑click `Run Sift Setup.command` on macOS. It will:
1) Ask for your Google OAuth Client ID and Secret
2) Generate `.env` for you
3) Create a Python virtual env
4) Install dependencies
5) Start the server and open http://localhost:8080/auth/start
